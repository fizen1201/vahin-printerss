# vahin-printerss

> This file provides context for Claude Code. It is auto-generated by WayPoint.

## Start Here

**Read `waypoint.manifest.yaml` first** - it contains:
- Current project state and document registry
- Specs, plans, and tasks with summaries and status
- Quick lookup without loading all files

## Project Overview

- **Type**: greenfield
- **Track**: team
- **Architecture**: Domain-Driven Design

## WayPoint Structure

```
waypoint.manifest.yaml   # PROJECT REGISTRY - Read this first!

.claude/commands/        # Slash commands (Claude Code integration)
├── waypoint.discover.md
├── waypoint.brainstorm.md
├── waypoint.constitute.md
├── waypoint.specify.md
├── waypoint.design.md
├── waypoint.clarify.md
├── waypoint.architect.md
├── waypoint.plan.md
├── waypoint.ux.md
├── waypoint.implement.md
├── waypoint.review.md
├── waypoint.extend.md
├── waypoint.test.md
├── waypoint.memory-query.md
├── waypoint.memory-seed.md
├── waypoint.pull.md
├── waypoint.memory-overview.md
└── waypoint.push.md
└── waypoint/agents/     # Agent personas
    ├── academy-tutor.md
    ├── architect.md
    ├── brainstorm.md
    ├── dev.md
    ├── modarch.md
    ├── module-architect.md
    ├── pm.md
    ├── product-manager.md
    ├── qa.md
    ├── qa-engineer.md
    ├── scrum-master.md
    ├── sm.md
    ├── tutor.md
    ├── ux.md
    └── ux-designer.md

.waypoint/
├── constitution.md      # Project principles
├── config.yaml          # Project configuration
├── templates/           # Document templates
├── workflows/           # Step-file workflows
│   ├── architect/
│   ├── design/
│   ├── discover/
│   ├── plan/
│   └── specify/
├── specs/               # Generated specifications
├── plans/               # Generated plans
└── tasks/               # Generated tasks
```

## Workflow Phases

1. **Discover** (`/waypoint.discover`) - Analyze existing codebase or explore problem space
2. **Brainstorm** (`/waypoint.brainstorm`) - Facilitate a creative brainstorming session using proven techniques
3. **Constitute** (`/waypoint.constitute`) - Review and customize the project constitution
4. **Specify** (`/waypoint.specify`) - Create a specification document for a feature
5. **Design** (`/waypoint.design`) - Create UX/UI design from a specification
6. **Clarify** (`/waypoint.clarify`) - Resolve ambiguities in a specification
7. **Architect** (`/waypoint.architect`) - Create a technical plan for a specification
8. **Plan** (`/waypoint.plan`) - Break down a plan into executable tasks
9. **/waypoint.ux Command** (`/waypoint.ux`) - 
10. **Implement** (`/waypoint.implement`) - Implement a task following project conventions
11. **Review** (`/waypoint.review`) - Review implementation for quality and compliance
12. **Extend** (`/waypoint.extend`) - Add new stacks, architectures, or modules to WayPoint
13. **Test** (`/waypoint.test`) - Testing
14. **Memory Query** (`/waypoint.memory-query`) - Query the Graphiti knowledge graph for project context
15. **Memory Seed** (`/waypoint.memory-seed`) - Seed architectural knowledge into waypoint-memory
16. **WayPoint Pull** (`/waypoint.pull`) - Pull configuration from WayPoint platform
17. **Memory Overview** (`/waypoint.memory-overview`) - Show an ASCII dashboard of the project's knowledge graph state
18. **Push Memory** (`/waypoint.push`) - Push local memory items to WayPoint platform

## Architecture: Domain-Driven Design

### Key Principles

- Domain logic is the heart of the application
- Entities have identity and lifecycle
- Value objects are immutable and compared by value

_See `.waypoint/constitution.md` for full principles._

### Anti-Patterns to Avoid

- **Anemic Domain Model**: Entities with only getters/setters, no behavior
- **Domain Layer Database Dependency**: Domain entities importing ORM decorators or database types
- **Leaking Domain Logic**: Business rules implemented in controllers or services outside domain

## Knowledge Management

This project uses two memory layers. Persona agents have specific READ/WRITE instructions for each.

- **WayPoint Memory** (Layer 2): Curated patterns, conventions, and solutions. Query via `waypoint memory search`.
- **Graphiti** (Layer 3): Temporal knowledge graph of decisions, requirements, and relationships. Query via `search_memory_facts` and `search_nodes` MCP tools. Store via `add_memory` MCP tool.

**Routing rule**: Repeatable instructions (patterns, conventions, error fixes) → WayPoint Memory. Decisions that evolve over time (architecture choices, requirements, trade-offs) → Graphiti.

## Memory Protocol

At natural breakpoints, check if any trigger applies. If none, continue working.

### Storing Knowledge

| When | Do |
|------|----|
| Decision with trade-offs | `add_memory(name="Decision: {topic}", episode_body="Decided X over Y because...", source_description="{persona} — recording decision")` |
| Reusable pattern or convention | `add_memory(name="Pattern: {topic}", episode_body="Use X pattern when...", source_description="{persona} — recording pattern")` |
| Non-obvious bug fix | `add_memory(name="Solution: {topic}", episode_body="When you see X, the root cause is Y...", source_description="{persona} — recording solution")` |
| Constraint or requirement | `add_memory(name="Requirement: {topic}", episode_body="Must always X because...", source_description="{persona} — recording requirement")` |
| Context about to be lost | `add_memory(name="Context: {topic}", episode_body="The current state is...", source_description="{persona} — preserving context")` |
| Bulk documentation or code content | `ingest_text(content="...", source_type="doc", group_id="vishesh-koul--vahin-printerss")` |

### Searching Knowledge

| When | Do |
|------|----|
| Need broad context | `search_hybrid(query="...", group_id="vishesh-koul--vahin-printerss")` |

### Maintenance

| When | Do |
|------|----|
| After seeding graph episodes | `build_communities(group_ids=["vishesh-koul--vahin-printerss"])` |

### Rules
- Keep episodes under 2000 chars
- Always tag yourself: source_description="{your persona} — {what and why}"
- Don't store: routine details, code snippets, test results

## Important Files

- `waypoint.manifest.yaml` - **Read this first** for project state and document registry
- `.waypoint/constitution.md` - Project principles and governance
- `.claude/commands/waypoint/agents/dev.md` - Contains code examples to follow
- `.waypoint/config.yaml` - Project configuration

---

_Generated by WayPoint_
_Architecture: Domain-Driven Design_
